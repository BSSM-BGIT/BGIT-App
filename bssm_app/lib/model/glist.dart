class GitRanklist {
  int rank;
  String profile;
  String userName;
  String name;
  int commit;
  bool isnull;

  GitRanklist(this.rank, this.profile, this.userName, this.name, this.commit, this.isnull);
}
